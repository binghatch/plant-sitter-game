class Interaction {
    constructor(config) {
        this.x = config.x,
        this.y = config.y,
        this.type = config.type,
        this.gameObject = config.gameObject,
        this.isVisible = config.spriteIsVisible
    }

    activate() {

    }
}